
describe('configure data', () => {
	describe('#value', () => {
		test.todo('base case of a `<text>` pseudo-selector');
		test.todo('base case of a simple attribute selector');
		test.todo('advanced case of a recursive-layered value');
		test.todo('advanced case of a recursive-layered value with additionally settings from sections below');
	});

	describe('#matches', () => {
		test.todo('base case of using `matches` where data is found');
		test.todo('base case of using `matches` where nothing is found');
	});

	describe('#closest', () => {
		test.todo('base case of using `closest` where data is found');
		test.todo('base case of using `closest` where nothing is found');
	});

	describe('#selector', () => {
		test.todo('base case of using `selector` where data is found');
		test.todo('base case of using `selector` where nothing is found');
	});

	describe('#expression', () => {
		test.todo('base case of using `expression` where data is found');
		test.todo('base case of using `expression` where nothing is found');
	});
});
